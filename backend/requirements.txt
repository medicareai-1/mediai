# Web Framework
Flask>=2.3.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Firebase
firebase-admin>=6.0.0

# OCR & Vision
pytesseract>=0.3.10
google-generativeai>=0.3.0

# Deep Learning (CPU-only versions to reduce size)
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0

# Image Processing
Pillow>=10.0.0
opencv-python-headless>=4.8.0

# PDF Processing
pymupdf>=1.23.0

# Machine Learning
scikit-learn>=1.3.0
numpy>=1.24.0,<2.0.0

# Explainability - Grad-CAM only (SHAP/LIME require scipy which needs Fortran compiler)
# NOTE: Render free tier doesn't have Fortran compiler, so scipy-based packages won't work
# Grad-CAM provides excellent visualization without these dependencies

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0
cryptography>=41.0.0
